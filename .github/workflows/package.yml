name: package build workflow
on:
  push:
    branches: [ master, github-build-action ]
  pull_request:
    branches: [ master ]

jobs:
  package_job:
    runs-on: ubuntu-latest
    name: A job to build the packages
    steps:
    - name: checkout
      uses: actions/checkout@v2
    - name: build it step
      id:   build-it-now
      #uses: richardmarston/arabica@64152a97
      uses: ./actions/
      with:
        commit: ${GITHUB_SHA}
        version: "0.1"
    - name: get artifacts
      run:  mkdir artifacts; rpmfile=$(find . -iname "*.rpm"); debfile=$(find . -iname "*.deb"); cp $rpmfile $debfile artifacts
    - name: upload artifacts
      uses: actions/upload-artifact@v2
      with:
        name: arabica-pkgs
        path: artifacts
